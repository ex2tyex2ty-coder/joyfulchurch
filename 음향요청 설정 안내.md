# r28 음향 요청 연결 안내

## 이번 배포 내용

참여자는 자유 별명으로 예배방에 입장하고, 버튼 또는 300자 이내 메시지를 보냅니다. 자기 요청과 음향팀 답변만 표시됩니다. 엔지니어는 별도 접근번호로 전체 요청을 보고 확인·조정 완료·답변·담당 인계를 합니다. 조정 후 참여자는 좋아요 또는 추가 조정 필요를 누릅니다.

예배방은 12시간 후 종료됩니다. 음향석 화면에서 먼저 종료할 수도 있습니다. 종료해도 기록은 지워지지 않습니다. 새로고침 후 같은 브라우저에서 개인 접속코드를 읽어 복귀하며, 브라우저 저장 차단 시 개인 복귀코드를 입력할 수 있습니다. 다른 기기에서 같은 별명을 입력해도 기존 메시지를 읽을 수 없습니다.

약 2초마다 활성 화면을 갱신하는 방식입니다. 실제 전달 시간은 네트워크와 서버 상태에 따라 달라집니다. 화면 잠금·백그라운드 알림은 지원하지 않습니다. 소리·진동 알림이나 실제 믹서/전원 장치 제어는 포함하지 않습니다.

## 1. 전용 PostgreSQL 저장소 준비

Supabase 등의 PostgreSQL 데이터베이스를 준비합니다. Supabase를 쓰는 경우 프로젝트의 Connect 화면에서 Session pooler 연결 문자열을 확인합니다. 데이터베이스 비밀번호를 넣어 완성한 연결 문자열은 Streamlit Secrets에만 저장합니다. 비밀번호에 특수문자가 있으면 URI 인코딩이 필요합니다.

공식 안내: https://supabase.com/docs/guides/database/connecting-to-postgres

기존 게시판 Google Sheets나 원본 두 Google Sheets에는 음향 요청을 저장하지 않습니다. 이 데이터베이스의 sound_로 시작하는 네 개 테이블만 사용합니다. 앱의 서버 DB 계정에는 테이블 생성·조회·수정 권한이 필요합니다. 최초 연결 시 테이블과 인덱스를 생성하고 RLS를 활성화합니다. 브라우저용 anon key 또는 service role API key가 아니라 PostgreSQL 연결 문자열을 사용합니다.

## 2. Streamlit Secrets 설정

기존 Secrets 내용은 그대로 두고 다음 두 값을 최상위에 추가합니다. [섹션명] 아래에 넣지 마세요.

```toml
AUDIO_DATABASE_URL = "postgresql://사용자:비밀번호@호스트:5432/postgres"
AUDIO_ENGINEER_PIN = "음향팀이 사용할 별도 접근번호"
```

실제 값은 GitHub와 채팅에 올리지 않습니다. 앱은 SSL로 연결합니다. 저장소 연결이 없으면 음향 메뉴에는 준비 중 안내만 표시됩니다. 기존 메뉴는 계속 사용할 수 있습니다.

## 3. 리허설 확인

1. 음향 요청 → 음향석 → 엔지니어 이름과 접근번호 입력.
2. 예배방 만들기 → 생성된 예배방 코드를 팀원에게 전달.
3. 각자 참여자 화면에서 코드와 별명(1번 마이크, 건반 등)으로 입장.
4. 6대의 폰에서 요청을 보내고 다른 참여자의 요청이 보이지 않는지 확인.
5. 음향석에서 확인·조정했어요를 누르고 해당 요청자에게만 답변이 오는지 확인.
6. 좋아요, 추가 조정, 취소, 엔지니어 인계, 새로고침 후 복귀를 확인.
7. 와이파이를 끊었다 연결한 뒤 전달 여부와 기존 요청 유지 여부를 확인.

## 함께 수정한 내용

- 팀 확인에서 진행할 일 / 해결 기록 / 현재 기준을 구분합니다.
- 보관한 이슈의 기준과 모든 댓글을 검색하고 읽을 수 있습니다.
- 전체 검색과 보관함에서 팀 확인 기록을 연결합니다(팀원/관리자 권한 적용).
- 빈 운영 기준을 저장하지 못하게 합니다.
- 일반 댓글의 기본값은 현재 상태 유지입니다. 해결은 별도 동작입니다.
- 실제 집계 횟수를 평균에 표시하고 온라인 미입력을 0으로 표시하지 않습니다.
- 업무 미등록을 모두 완료했다고 표현하지 않습니다.
- 접속 시 1시간 이상 지난 원본 Sheets와 설정된 교회력을 읽기 전용으로 갱신합니다. 실패 시 10분 후 재시도 가능하며 앱이 닫혀 있을 때의 예약 실행은 아닙니다.
- 자동 원본 갱신을 끄려면 최상위 Secret AUTO_SOURCE_REFRESH = "false"를 추가합니다.

## 아직 별도로 완료해야 하는 작업

음향 요청 PostgreSQL 연결은 기존 행사·매뉴얼·결정·운영로그의 저장 방식을 바꾸지 않습니다. 이 기록들은 여전히 로컬 SQLite를 사용합니다. 해당 데이터의 영구 DB 이관과 자동 백업·복원은 다음 작업으로 남아 있습니다. 중요한 기존 기록은 데이터·백업 메뉴에서 내려받아 보관하세요.

코드 테스트는 SQLite 테스트 저장소 및 Streamlit AppTest로 수행했습니다. 실제 PostgreSQL 연결과 현장 폰 6대 동시 수신은 연결정보 설정 후 검증해야 합니다.
