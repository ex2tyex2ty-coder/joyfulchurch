# r37 · 사람별 대화와 보관

## 동작

A가 '모니터가 안 들려요', '반주 올려주세요'를 보내면 A의 대화창 하나에
시간순으로 두 요청이 표시됩니다. 음향석은 A의 대화창에서 답변 한 번을 작성합니다.
다른 참여자에게 A의 대화는 보이지 않습니다. 별명이 아니라 개인 접속 식별자로 구분합니다.

음향석의 버튼:
- 확인 · 처리 중: 현재 대화의 미처리 요청을 함께 확인합니다.
- 현재 요청 조치 완료: 현재 보이는 미처리 요청을 함께 완료 처리합니다.
- 답변 보내기: 대화 안에 답변 하나를 남깁니다. 완료로 자동 변경하지 않습니다.
- 처리 완료·보관: 현재 미처리 요청을 모두 완료 처리하고 진행 목록에서 숨깁니다.
- 대화 보관하기: 미처리 요청이 없는 대화를 진행 목록에서 숨깁니다.
- 보관함 → 대화 불러오기: 기록을 그대로 유지하며 진행 목록으로 돌려놓습니다.

각 대화 제목을 눌러 접거나 펼칠 수 있습니다. 대화 안의 기록은 시간순이고
새 내용은 아래에 표시됩니다. 긴 대화는 내부에서 스크롤합니다.
부분적으로만 처리한 경우에는 답변으로 상황을 남기고, 전체 처리가 끝났을 때
'현재 요청 조치 완료' 또는 '처리 완료·보관'을 눌러주세요.

참여자의 '좋아요 · 확인했어요', '추가 조정 필요', '현재 요청 모두 취소'도
대화 전체의 해당 상태 요청에 적용합니다. 버튼별 별도 처리창은 없앴습니다.

## 보관은 삭제가 아닙니다

영구 삭제는 이번에 추가하지 않았습니다. 요청과 답변은 그대로 남습니다.
보관 상태는 음향석의 진행 목록에만 적용됩니다. 참여자는 자신의 대화를 계속 읽습니다.
보관한 사람이 새 요청을 보내거나 추가 조정을 요청하면 같은 대화가 진행 목록으로
자동 복귀합니다. 동일한 사람에게 새 대화창을 만들지 않습니다.
새 요청이 도착한 뒤 예전 화면에서 완료/보관을 누르면 충돌 안내를 표시합니다.
새로고침해서 새 요청까지 확인한 다음 다시 처리해주세요.

## 종료된 예배방

음향석의 예배방 선택 목록에는 종료된 방도 표시합니다. 종료된 방에서도
기록 조회와 보관/불러오기가 가능합니다. 방을 다시 여는 기능은 아니므로
종료된 방에서 새 요청이나 답변을 작성할 수는 없습니다.
실제 데이터가 유지되는 동안 언제든 기록을 다시 볼 수 있습니다.

## 업로드

joyful_r37_person_chats.zip 하나만 풀어서 내부 파일과 폴더를 모두 업로드합니다.
audio_conversations.py와 audio_chat_ui.py가 새로 추가되므로 반드시 함께 올립니다.
audio_realtime.sql도 수정되어 새 대화 답변의 실시간 변경 알림을 지원합니다.
기존 Secrets는 유지합니다. 배포 후 이전 코드가 남아 있으면 한 번 Reboot하세요.

첫 실행 시 sound_thread_messages 테이블과 sound_people의 desk_archived 필드를
추가합니다. 기존 요청/답변은 삭제하지 않고 새 대화 기록과 합쳐 표시합니다.
새 테이블도 RLS를 적용하며 브라우저에 직접 공개하지 않습니다.
원본 Google Sheets와 기존 팀 확인 게시판은 수정하지 않습니다.

로컬 DB 테스트, AppTest, 실시간 모의 테스트를 수행합니다.
실제 Supabase SQL 설치와 휴대폰 간 송수신/접기 상태는 배포 후 검증해야 합니다.
Supabase 실시간 설정이 아직 안 된 경우에는 요청·답변 새로고침을 눌러 확인합니다.
