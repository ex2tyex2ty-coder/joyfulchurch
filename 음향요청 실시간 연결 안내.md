# r29 · 요청이 생길 때만 반영하는 음향 요청

주기적인 DB 조회와 화면 새로고침을 제거했습니다. 버튼 조작, 새 알림,
첫 연결, 연결 복구, 화면 복귀 시에만 목록을 조회합니다.
WebSocket 연결 유지용 신호(25초)와 인증 갱신은 필요하지만,
연결 유지 신호는 DB 목록을 조회하거나 Streamlit 화면을 갱신하지 않습니다.

## 업로드

ZIP을 풀어 안의 파일과 sound_realtime 폴더를 기존 app.py와 같은 위치에 업로드합니다.
sound_realtime/index.html과 sound_realtime/realtime.js가 폴더 안에 있어야 합니다.
requirements.txt도 반드시 업로드합니다. 기존 DB 비밀번호와 구글 설정은 그대로 둡니다.

## Supabase에서 한 번 설정

1. 해당 프로젝트의 Authentication 설정에서 Anonymous Sign-Ins(익명 로그인)를 켭니다.
   이는 별도의 가입 화면 없이 수신자 인증을 하는 용도입니다.
   참고: https://supabase.com/docs/guides/auth/auth-anonymous
2. 프로젝트의 Connect 또는 API Keys 화면에서 Publishable key를 확인합니다.
   sb_publishable_로 시작하는 키입니다. sb_secret_ 또는 service_role 키가 아닙니다.
3. Streamlit → Settings → Secrets의 **맨 위**에 다음 두 줄을 추가합니다.
   기존 두 AUDIO 설정이나 Google 설정을 지우지 마세요.

```toml
AUDIO_SUPABASE_URL = "https://프로젝트번호.supabase.co"
AUDIO_SUPABASE_PUBLISHABLE_KEY = "sb_publishable_로시작하는실제키"
```

프로젝트번호는 Session pooler 사용자명의 postgres. 뒤 프로젝트번호와 같아야 합니다.
Project URL에는 :5432, /postgres, DB 비밀번호를 넣지 않습니다.
4. Supabase Realtime 설정에서 Allow public access를 끄는 것을 권장합니다.
   이 앱은 private 채널만 사용합니다. 다른 앱이 같은 프로젝트의 공개 채널을 사용한다면
   영향을 확인한 뒤 변경하세요.
5. 앱에서 음향 요청으로 입장합니다. 첫 수신 화면 접속 시 동봉 SQL을 서버가 설치합니다.
   성공하면 작은 '실시간 수신 연결됨' 표시가 나옵니다.

## 동작과 권한

- 익명 Auth 사용자를 확인한 뒤, 서버가 참여자에게 자신의 채널만 부여합니다.
- 엔지니어는 기존 음향석 비밀번호 인증 후 선택한 예배방 채널만 받습니다.
- 브라우저에는 공개 키와 본인 Auth 토큰만 전달합니다. DB 비밀번호는 보내지 않습니다.
- private 채널에는 빈 변경 신호만 보냅니다. 요청 본문·별명·복귀코드는 넣지 않습니다.
- 실제 요청/답변은 기존 AudioStore의 본인 확인 또는 음향석 권한 검사 후 읽습니다.
- 채널 접속 권한은 JWT 갱신 시 다시 확인합니다. 로그아웃 시 컴포넌트 연결은 종료됩니다.
- 방 종료나 권한 만료 이후 서버에서 새 작업은 거절합니다.
- 인증정보는 같은 탭의 sessionStorage에 보관합니다. 요청 내용은 보관하지 않습니다.
- Realtime 설정이 없거나 실패하면 수동 새로고침만 제공합니다. 주기적 조회로 몰래 대체하지 않습니다.

## 설치되는 DB 객체

기존 sound_* 데이터는 지우지 않습니다. sound_receivers(수신 권한) 테이블,
joyful_sound_* 이름의 함수·트리거·정책을 설치/갱신합니다.
realtime.messages의 다른 앱 정책은 삭제하지 않습니다. sound: 채널에는 본인 권한 확인을
추가로 요구하고 브라우저 발신을 금지하는 제한 정책을 설치합니다.
트리거는 DB 저장과 함께 변경 신호를 기록합니다. 알림 생성에 실패하면 해당 저장도
롤백되어 성공했다고 잘못 표시하지 않습니다. DB 서버 계정에는 설치 권한이 필요합니다.
원본 Google Sheets와 팀 확인 게시판 저장소는 수정하지 않습니다.

## 실제 사용 전 확인

1. 휴대폰 두 대 이상에서 서로 다른 별명으로 입장합니다.
2. 1번의 요청은 음향석과 1번 화면에서만 보이는지 확인합니다.
3. 조정 완료·개인 답변이 해당 참여자에게만 도착하는지 확인합니다.
4. 아무 작업 없이 기다릴 때 화면이 반복해서 흐려지지 않는지 확인합니다.
5. 와이파이 끊기/복구와 화면 잠금 후 복귀 때 놓친 요청이 표시되는지 확인합니다.
6. 방 종료, 잘못된 코드, 음향석 로그아웃 후 접근 제한을 확인합니다.
7. 연결 실패 시 '실시간 수신 다시 연결' 또는 수동 새로고침을 사용합니다.

## 제한 및 검증 범위

폰 잠금 중 푸시 알림은 아닙니다. 화면을 열어둔 상태의 실시간 알림입니다.
네트워크·Supabase 요금제 제한에 따라 지연/차단될 수 있어 즉시 도착을 보장하지 않습니다.
Anonymous Sign-Ins는 Supabase Auth에 익명 사용자 레코드를 만듭니다.
새 탭·새 브라우저의 반복 접속은 레코드와 인증 사용량을 늘립니다.
공개 운영 시 익명 가입 남용 방지와 Auth 요금/제한을 확인해야 합니다.
현재 클라이언트는 CAPTCHA 입력을 지원하지 않습니다. CAPTCHA가 필수인 프로젝트에서는
수신 인증이 실패하므로 CAPTCHA UI 추가 후 활성화해야 합니다.

로컬 Python/AppTest와 모의 WebSocket 테스트를 수행했습니다.
실제 Supabase SQL 실행·인증·Realtime 채널과 모바일 브라우저 연동은 배포 설정 후
위 리허설로 검증해야 합니다. 현장 검증 전 실시간 연결 완료로 간주하지 마세요.

공식 자료:
- https://supabase.com/docs/guides/realtime/authorization
- https://supabase.com/docs/guides/realtime/broadcast
- https://supabase.com/docs/guides/realtime/protocol
